services:
  gocast:
    build: .
    ports:
      - 8080:8080
      - 8000:8000

  foo:
    image: jrottenberg/ffmpeg
    command: |
      -hide_banner
      -re -f lavfi -i sine
      -f mp3 http://gocast:8080/streams/foo
    depends_on:
      - gocast

  bar:
    image: jrottenberg/ffmpeg
    command: |
      -hide_banner
      -re -f lavfi -i sine
      -f adts icecast://gocast:8000/bar
    depends_on:
      - gocast

  bar-leg:
    image: jrottenberg/ffmpeg
    command: |
      -hide_banner
      -re -f lavfi -i sine
      -f adts -legacy_icecast 1 icecast://gocast:8000/bar-leg
    depends_on:
      - gocast

  baz:
    image: jrottenberg/ffmpeg
    command: |
      -hide_banner
      -re -f lavfi -i sine
      -f adts srt://gocast:6000?streamid=pub:baz
    depends_on:
      - gocast
